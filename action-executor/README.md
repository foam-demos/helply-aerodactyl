# Action Executor

Executes real customer actions: refunds via Stripe, plan changes, billing updates, order status checks, and invoice generation. Built with TypeScript, Node.js, and idempotency keys for safe retries.

## Local Development
```bash
npm install
npm run dev
```

Requires Stripe API keys and access to customer help desk APIs.